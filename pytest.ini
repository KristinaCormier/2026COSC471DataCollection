[pytest]
minversion = 7.0
testpaths = tests
addopts = -ra
markers =
    unit: fast pure-logic tests
    integration: tests that hit Postgres or external systems
    pipeline: end-to-end model training/inference tests
