# 2026COSC471DataCollection

current dependencies:
Requests
Pandas
OS
Time

The script currently runs using Google Collab. 

That seems to be an issue as it stops running every 12h.

We would like to modify the script for Juypter notebook to overcome the 12h issue, currently lacking some dependencies

The script collects data for two stocks for now, open and close for current data was verified manually.

It automatically downloads 5 min stock price data from FMV and keeps a .csv file up to date by appending data every 5 minutes
